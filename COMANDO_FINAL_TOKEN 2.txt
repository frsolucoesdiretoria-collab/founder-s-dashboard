# 🚀 COMANDO FINAL - CONFIGURAR TOKEN E TESTAR
# Copie e cole este comando COMPLETO na VPS:

cd /var/www/founder-dashboard && cp .env.local .env.local.backup && sed -i 's/NOTION_TOKEN=SET_NOTION_TOKEN_AXIS_HERE/NOTION_TOKEN=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/' .env.local && echo "✅ Token configurado!" && grep "^NOTION_TOKEN=" .env.local | head -c 60 && echo "..." && pm2 restart founder-dashboard && sleep 8 && echo "" && echo "=== TESTE KPIs ===" && curl -s http://localhost:3001/api/kpis/public && echo "" && echo "" && echo "Quantidade:" && curl -s http://localhost:3001/api/kpis/public | grep -o '"id"' | wc -l && echo "" && echo "=== LOGS ===" && pm2 logs founder-dashboard --lines 15 --nostream | tail -10





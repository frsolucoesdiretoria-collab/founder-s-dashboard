name: Deploy to VPS

on:
  push:
    branches:
      - staging
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  deploy:
    name: Deploy to ${{ github.ref == 'refs/heads/main' && 'Production' || 'Staging' }}
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --include=dev

      - name: Build application
        run: npm run build

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            
            # Determine project path based on branch
            if [ "${{ github.ref }}" == "refs/heads/main" ]; then
              PROJECT_PATH="${{ secrets.VPS_PROJECT_PATH }}"
              ENV="production"
            else
              PROJECT_PATH="${{ secrets.VPS_STAGING_PATH || secrets.VPS_PROJECT_PATH }}"
              ENV="staging"
            fi
            
            echo "🚀 Deploying to $ENV environment at $PROJECT_PATH"
            
            cd $PROJECT_PATH
            
            # Pull latest changes (discard local changes and sync with remote)
            echo "📥 Pulling latest changes from ${{ github.ref_name }}..."
            git fetch origin
            git reset --hard origin/${{ github.ref_name }}
            git clean -fd
            
            # Configure .env.local with Enzo database IDs
            echo "⚙️  Configuring .env.local..."
            if [ ! -f .env.local ]; then
              echo "   Creating .env.local from env.local.example..."
              cp env.local.example .env.local
            fi
            
            # Ensure Enzo database IDs are set (from env.local.example)
            if ! grep -q "NOTION_DB_KPIS_ENZO=" .env.local; then
              echo "   Adding Enzo database IDs to .env.local..."
              echo "" >> .env.local
              echo "# Enzo Canei Dashboard Databases" >> .env.local
              echo "NOTION_DB_KPIS_ENZO=2ed84566a5fa818b8b0d000c657c9913" >> .env.local
              echo "NOTION_DB_GOALS_ENZO=2ed84566a5fa812d8244000c3a5cdf24" >> .env.local
              echo "NOTION_DB_ACTIONS_ENZO=2ed84566a5fa8182ab81000c1905c93e" >> .env.local
            else
              echo "   Enzo database IDs already configured in .env.local"
            fi
            
            # Install dependencies (including devDependencies for build)
            echo "📦 Installing dependencies..."
            npm ci --include=dev
            
            # Build application
            echo "🔨 Building application..."
            NODE_ENV=production npm run build
            
            # Restart application (PM2)
            if command -v pm2 &> /dev/null; then
              echo "🔄 Restarting application with PM2..."
              # Parar e remover processo existente
              pm2 stop founder-dashboard 2>/dev/null || true
              pm2 delete founder-dashboard 2>/dev/null || true
              # Matar processo na porta 3001 se existir
              lsof -ti:3001 | xargs kill -9 2>/dev/null || true
              sleep 2
              # Iniciar com NODE_ENV=production explicitamente
              NODE_ENV=production pm2 start npm --name "founder-dashboard" -- start
              pm2 save
              pm2 logs founder-dashboard --lines 20 --nostream
            else
              echo "⚠️  PM2 not found. Please restart the application manually."
            fi
            
            echo "✅ Deployment completed successfully!"

      - name: Notify deployment status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment to ${{ github.ref == 'refs/heads/main' && 'Production' || 'Staging' }} completed successfully!"
          else
            echo "❌ Deployment failed!"
            exit 1
          fi


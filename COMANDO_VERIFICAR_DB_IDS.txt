cd /var/www/founder-dashboard && echo "=== DATABASE IDs CONFIGURADOS ===" && grep "^NOTION_DB_" .env.local && echo "" && echo "=== TESTE DIRETO COM API DO NOTION ===" && TOKEN=$(grep "^NOTION_TOKEN=" .env.local | cut -d'=' -f2) && DB_KPIS=$(grep "^NOTION_DB_KPIS=" .env.local | cut -d'=' -f2) && echo "Token: ${TOKEN:0:30}..." && echo "Database KPIs ID: $DB_KPIS" && echo "" && if [ ! -z "$DB_KPIS" ]; then curl -s -X POST "https://api.notion.com/v1/databases/$DB_KPIS/query" -H "Notion-Version: 2022-06-28" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{}' | head -c 1000 && echo ""; else echo "❌ NOTION_DB_KPIS não encontrado no .env.local"; fi && echo "" && echo "=== REINICIANDO E TESTANDO API LOCAL ===" && pm2 restart founder-dashboard --update-env && sleep 10 && curl -s http://localhost:3001/api/kpis/public && echo "" && echo "" && echo "Quantidade:" && curl -s http://localhost:3001/api/kpis/public | grep -o '"id"' | wc -l




